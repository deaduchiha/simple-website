import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "lato",
    fontWeightLight: 300,
    fontWeightRegular: 400,
    fontWeightBold: 700,
  },
});
